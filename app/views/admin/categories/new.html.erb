<h1>Fill in the details of your category below</h1>

<%= form_for [:admin, @category] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>

<% end %>