<h1>Admin Categories</h1>

<% @categories.each do |category|%>
  <p><%= category.title %>: <%= category.description %> <%= link_to "Delete", admin_category_path(category), method: :delete %> </p>
<% end %>

<%= link_to "Create new category", new_admin_category_path %>